# Emotion Detection App (IBM Watson + Flask + Docker)

This project uses IBM Watson NLP to detect emotions from text input.

## Features
- Emotion detection (joy, anger, sadness, fear, disgust)
- Flask REST API
- Docker deployment
- Unit testing